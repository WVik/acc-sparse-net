mapper:
  optimization-metrics: [ delay, energy ]
  live-status: False
  num-threads: 8
  timeout: 15000
  victory-condition: 500
  algorithm: random-pruned
  max-permutations-per-if-visit: 16
  diagnostics: False # fails to find valid mappings if diagnostics is off apparently ?
